{
  "name": "zh-mock",
  "version": "0.0.1-alpha",
  "description": "这是一个为前端程序员API调试的快捷MOCK工具",
  "main": "index.js",
  "repository": "https://github.com/ITzhangxi/zh-mock-jq.git",
  "author": "zackxizi <zackxizi@qq.com>",
  "license": "MIT",
  "scripts": {
    "dev": "rollup -c -w",
    "build": "yarn cp;yarn build:chrome;yarn build:panel",
    "cp": "rm -rf dist;mkdir -p dist;cp manifest.json dist/manifest.json;cp -r _locales dist/_locales;cp -r img dist/img;cp -rf pages dist;rm -rf dist/pages/panel",
    "build:chrome": "yarn cp; rollup -c rollup.config.build.js",
    "build:panel": "mkdir -p dist/pages/panel/dist;cd pages/panel;cp -r dist ../../dist/pages/panel"
  },
  "devDependencies": {
    "rollup": "^2.60.1",
    "rollup-plugin-terser": "^7.0.2"
  }
}
